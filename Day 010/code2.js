/*
Q2: Why do we not need to specify if(isLearning === true)? Why does if(isLearning) work on its own
*/
/* solution:
javascript himself set the value of the variable that's why if(isLearning) work on its own.
*/
