/*
Q6 : When to Use if else vs ternary vs switch?
*/

/*
solution:
We use if else when there are alteast more than two conditions
We use ternary when the condition is too simple.
When there are a lot of conditions it is better to use switch instead of if else
*/
